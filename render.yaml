services:
  - type: web
    name: sentinel-research-v2
    runtime: python
    branch: animated-login
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
